{% macro navpost(item, type) %}
  {%- set postText = item.title or item.link or __('post.untitled') %}
  {%- set lastcat = item.categories.last() %}

  {%- set itemlink %}
  <span class="type">{{__('post.' + type)}}</span>
  <span class="category"><i class="ic i-flag"></i> {{lastcat.name}}</span>
  <h3>{{ postText }}</h3>
  {% endset %}

  {%- set navbg = _cover(item) %}
  {%- if navbg %}
  {{ _url(item.path, itemlink, {itemprop: 'url', rel: type, 'data-background-image': navbg, title: postText}) }}
  {%- else %}
  {{ _url(item.path, itemlink, {itemprop: 'url', rel: type, title: postText, class: 'post-nav-link--empty'}) }}
  {%- endif %}
{% endmacro %}

<div class="post-nav">
    <div class="item left">
      {%- if post.next %}
      {{navpost(post.next, 'prev')}}
      {%- endif %}
    </div>
    <div class="item right">
      {%- if post.prev %}
      {{navpost(post.prev, 'next')}}
      {%- endif %}
    </div>
</div>
